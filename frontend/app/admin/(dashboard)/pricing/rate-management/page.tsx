// "Rate Management" is the task-framed entry point to the same Provider -> Country -> Weight
// Category -> Rate drill-down as the Providers grid — rather than building a second, divergent
// provider picker for the same underlying task, this section renders that exact page.
export { default } from "../providers/page";
